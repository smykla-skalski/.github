---
name: Sync Labels

on:
  push:
    branches: [main]
    paths:
      - ".github/labels.yml"
      - ".github/workflows/sync-labels.yml"
      - "action.yml"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no changes)"
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      repo:
        description: "Single repository to sync (optional, syncs all if not provided)"
        required: false
        type: string
        default: ""
      dry_run:
        description: "Dry run (no changes)"
        required: false
        type: boolean
        default: false
    secrets:
      SMYKLOT_APP_PRIVATE_KEY:
        required: true

permissions: {}

jobs:
  get-repos:
    runs-on: ${{ fromJSON(vars.RUNNER_LABEL || '["ubuntu-24.04"]') }}
    outputs:
      repos: ${{ steps.repos.outputs.repos }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Generate App Token
        id: token
        if: inputs.repo == ''
        uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf # v2.2.1
        with:
          app-id: ${{ vars.SMYKLOT_APP_ID }}
          private-key: ${{ secrets.SMYKLOT_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Get repositories
        id: repos
        uses: ./.github/actions/get-repos
        with:
          repo: ${{ inputs.repo }}
          token: ${{ steps.token.outputs.token }}

  sync-labels:
    needs: get-repos
    runs-on: ${{ fromJSON(vars.RUNNER_LABEL || '["ubuntu-24.04"]') }}
    concurrency:
      group: sync-labels-${{ matrix.repo.name }}
      cancel-in-progress: false
    strategy:
      fail-fast: false
      matrix:
        repo: ${{ fromJSON(needs.get-repos.outputs.repos) }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Generate App Token
        id: token
        uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf # v2.2.1
        with:
          app-id: ${{ vars.SMYKLOT_APP_ID }}
          private-key: ${{ secrets.SMYKLOT_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ matrix.repo.name }}

      - name: Sync labels to repository
        uses: ./
        with:
          command: labels
          subcommand: sync
          token: ${{ steps.token.outputs.token }}
          repo: ${{ matrix.repo.name }}
          labels_file: .github/labels.yml
          dry_run: ${{ inputs.dry_run || 'false' }}
          result_file: labels-result-${{ matrix.repo.name }}.json

      - name: Upload result artifact
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: labels-result-${{ matrix.repo.name }}
          path: labels-result-${{ matrix.repo.name }}.json
          retention-days: 1

  summary:
    needs: sync-labels
    if: always()
    runs-on: ${{ fromJSON(vars.RUNNER_LABEL || '["ubuntu-24.04"]') }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Download all result artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          pattern: labels-result-*
          path: results

      - name: Flatten results directory
        run: mv results/labels-result-*/labels-result-*.json results/ 2>/dev/null || true

      - name: Generate markdown summary
        uses: ./
        with:
          command: summary
          subcommand: generate
          type: labels
          results_dir: results
          format: markdown
          dry_run: ${{ inputs.dry_run || 'false' }}

      - name: Generate workflow summary JSON
        uses: ./
        with:
          command: summary
          subcommand: generate
          type: labels
          results_dir: results
          format: json
          output: summary-labels.json
          dry_run: ${{ inputs.dry_run || 'false' }}

      - name: Upload workflow summary artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: workflow-summary-labels
          path: summary-labels.json
          retention-days: 7
